<!DOCTYPE HTML>
<html lang="es">
    <head>
        <meta charset="utf-8" />
        <title>
            {% block title %} Weddings {% endblock %}
        </title>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" />
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
    </head>
    <body>
        <header>
            <nav class="navbar navbar-default">
                <div class="container-fluid">
                    <!-- Brand and toggle get grouped for better mobile display -->
                    <div class="navbar-header">
                        <a class="navbar-brand" href="{{path("wedding_homepage")}}">Weddings</a>
                    </div>
                    
                    <!-- Collect the nav links, forms, and other content for toggling -->
                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            {% block menu %}
                                {% if is_granted('ROLE_ADMIN')%}
                                  <li><a href="{{path("wedding_index_wedding")}}">Bodes</a></li>
                                  <li class="{% block dishes %} {% endblock %}"><a href="{{path("wedding_index_dish")}}">Plats <span class="sr-only"></span></a></li>
                                {%endif%}
                                {%if is_granted('ROLE_USER')%}
                                  <li><a href="{{path("wedding_index_wedding")}}">Bodes</a></li>
                                  
                                {%endif%}
                                
                            {% endblock %}
                        </ul>
                        
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="#">
                                {% if app.user != null %}
                                    Benvingut/da {{app.user.name}} {{app.user.surname}}
                                {%endif%}
                                </a>
                            </li>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <span class="glyphicon glyphicon-cog"></span> <span class="caret"></span></a>
                                <ul class="dropdown-menu">
                                    {% if app.user == null %}
                                        <li><a href="{{path("login")}}">Entrar</a></li>
                                    {%else%}
                                        <li><a href="{{path("logout")}}">Sortir</a></li>
                                    {%endif%}
                                        <li><a href="#">Ajuda</a></li>
                                </ul>
                            </li>
                        </ul>
                    </div><!-- /.navbar-collapse -->
                </div><!-- /.container-fluid -->
            </nav>
        </header>
        <section id="content">
            {% block content %} {% endblock %}
        </section>
        <footer>
            David Martinez
        </footer>
    </body>
</html>
